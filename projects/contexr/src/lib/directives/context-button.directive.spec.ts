import { ContextButtonDirective } from './context-button.directive';

describe('ContextButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ContextButtonDirective();
    expect(directive).toBeTruthy();
  });
});
